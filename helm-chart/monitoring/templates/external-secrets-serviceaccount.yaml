# This file is currently disabled as we're using direct Kubernetes Secrets
# Uncomment to use ExternalSecrets
#
# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   name: external-secrets-monitoring
#   namespace: {{ .Release.Namespace }}
#   annotations:
#     eks.amazonaws.com/role-arn: arn:aws:iam::163459217187:role/{{ .Values.clusterName }}-external-secrets
# ---
# # Use the existing service account for the External Secret
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: aws-secretsmanager
#   namespace: {{ .Release.Namespace }}
# spec:
#   provider:
#     aws:
#       service: SecretsManager
#       region: {{ .Values.global.region | default "us-east-1" }}
#       auth:
#         jwt:
#           serviceAccountRef:
#             name: external-secrets-monitoring