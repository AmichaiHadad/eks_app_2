\
  data:
  - secretKey: webhookUrl
    remoteRef:
      key: ${secret_name} # Use the AWS Secret name passed from TF
      property: webhookUrl # Assuming the JSON in AWS SM has {\"webhookUrl\": \"...\"} 